I am building a CMS-based website on Node.js using Replit.

My goal is to ensure that:

All new pages

All old pages

All blog posts

Are fully crawlable and readable by Googlebot without JavaScript execution issues.

Fix the entire architecture using the following strict rules:

1️⃣ SERVER-SIDE RENDERING ONLY (NO SPA)

Do NOT use React SPA or client-side rendering.

Use Express + EJS (or similar template engine).

When I open:

view-source:https://mydomain.com/blog/post-slug

I must see full HTML content including:

<h1>Title</h1> <p>Full text content</p>

Google must not depend on JavaScript to see content.

No fetch-based content loading.
No dynamic DOM injection.

Everything must be rendered before sending to browser.

2️⃣ ENSURE GOOGLEBOT CAN ACCESS SITE

Check and confirm:

robots.txt exists

robots.txt returns HTTP 200

robots.txt contains:

User-agent: *
Allow: /
Sitemap: https://mydomain.com/sitemap.xml

No disallow rules.

3️⃣ AUTO DYNAMIC SITEMAP SYSTEM

Create dynamic route:

/sitemap.xml

It must automatically include:

All pages

All blog posts

Updated <lastmod> timestamps

No duplicate URLs

Whenever new page or post is published:

Sitemap must update automatically

Ensure sitemap returns valid XML format and HTTP 200.

4️⃣ STRONG INTERNAL LINKING STRUCTURE

Automatically ensure:

Every new blog appears on /blog page

Every page linked in footer

Create /all-pages page listing all URLs

Add breadcrumb navigation

No orphan pages allowed.

5️⃣ URL STRUCTURE

Use clean SEO URLs:

/about
/privacy-policy
/blog/how-to-download-instagram-reels

No query strings.
No hash routing.
No # routes.

6️⃣ AUTO GOOGLE DISCOVERY

After publishing content:

Regenerate sitemap

Send GET request to:

https://www.google.com/ping?sitemap=https://mydomain.com/sitemap.xml

https://www.bing.com/ping?sitemap=https://mydomain.com/sitemap.xml

Log status.

7️⃣ ENSURE PAGES RETURN 200 STATUS

Check:

No 302 redirect chains

No 404

No soft 404

No blocked routes

All published content must return HTTP 200.

8️⃣ REMOVE JAVASCRIPT DEPENDENCY

Do NOT load main content via:

fetch()
axios
AJAX

Content must be in initial HTML response.

9️⃣ PERFORMANCE OPTIMIZATION

Enable:

gzip compression

caching headers

fast TTFB

no blocking scripts

Target:
Response time under 500ms.

🔟 INDEX READINESS CHECK FUNCTION

Before publishing new page, validate:

Meta description exists

One H1 only

Canonical tag correct

No meta noindex

Minimum 600 words content

If failed → show warning in admin dashboard.

FINAL GOAL

When I create new page or blog:

It gets clean URL

It renders server-side

It appears in sitemap

It appears in blog listing

It appears in footer/all-pages

Google is pinged

Page returns HTTP 200

Page contains full HTML in view-source

No JavaScript-only content allowed.

Fix entire project to meet these requirements.

🔥 END PROMPT

🧠 Now Let Me Explain Something Important

Even after ALL this:

Google may NOT index immediately.

Because indexing depends on:

Domain authority

Crawl budget

Content quality

Backlinks

But with this architecture:

There will be ZERO technical problem.

🔎 How YOU Check If It’s Fixed

After Replit updates:

1️⃣ Open:
view-source:https://yourdomain.com/blog/post-slug

If you see full HTML content → GOOD.

If you see empty div → BAD.

2️⃣ Open:
https://yourdomain.com/robots.txt

It must load.

3️⃣ Open:
https://yourdomain.com/sitemap.xml

It must show all URLs.

4️⃣ In Google Search Console
Use “Test Live URL”.

If crawl allowed = Yes
Then problem is authority, not structure.

🚨 Final Truth

Your issue is 80% architecture + 20% authority.

Fix architecture once.
Then build backlinks gradually.